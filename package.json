{
  "name": "image-cache-cloudfront",
  "version": "0.0.1",
  "description": "Boilerplate for a CloudFront image cache",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "zip": "rm dist/ -rf && mkdir dist/ && cp -a src/ dist/src && cp package.json dist/ && cd dist && pnpm install --node-linker=hoisted --prod && zip -r -9 ../dist_cache-cloudfront.zip ."
  },
  "keywords": ["cloudfront", "image", "cache", "boilerplate"],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.22.0",
  "dependencies": {
    "sharp": "0.34.5"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "3.930.0",
    "@types/aws-lambda": "8.10.158",
    "@types/node": "24.10.1",
    "eslint": "9.39.1",
    "neostandard": "0.12.2",
    "tsc-alias": "1.8.16",
    "tsx": "4.20.6",
    "typescript": "5.9.3"
  }
}